# AGENTS.md

このリポジトリは、GitHub Pagesで動く静的クイズテンプレートです。

## 方針

- React, Vite, TypeScript, npm は使わない
- HTML / CSS / JavaScript のみで実装する
- 問題データは `questions.js` に分離する
- アプリの処理は `app.js` に書く
- デザインは `style.css` に書く
- `index.html` は画面構造を中心にする

## 目標

- `questions.js` だけ編集すれば別科目のクイズに流用できる構造にする
- 4択・5択など、選択肢数が変わっても動くようにする
- 解答後に正誤表示と解説欄を出す
- スマホでも見やすくする

## 実装済みの主な機能

- 1つ選択: `type: "single"`
- 複数選択: `type: "multi"`
- 正誤判定
- 解説表示
- スコア表示
- 進捗表示
- カテゴリ絞り込み
- シャッフル
- リセット

## 注意

- 問題データの構造を変える場合は README も更新する
- 既存の授業名に依存する文言を増やさない
- テンプレートとして再利用しやすい状態を保つ
